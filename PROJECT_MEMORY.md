# 项目持久记忆 Project Memory

> 本文件由 dsh-memoir 插件维护：记录本项目历次会话的工作归纳、经验教训与行动指南，
> 作为未来 AGENTS 接手本项目时的行动指南；它是人类可读的投影，不是 system prompt 的完整注入内容。
> 新会话只注入有界的 Hot Memory，完整历史通过 memoir_read 按需检索。

## 工作记录 Work Log

- [2026-09-16 11:18] [工作记录] 回滚 main 到 6add38e 清除素材污染 — main 分支被远程推送的 5 个提交（bf1415a~924a155）污染，混入了旧素材（materials）代码和 v0.2 骨架的混合物。回滚到 6add38e（feat: integrate Milkdown editor with local file autosave），清除了素材污染，保留了 Milkdown 编辑器、基本 index.tsx 入口和 host 插件骨架。
- [2026-09-16 11:37] [工作记录] 定位创建工作区 default/sift 类型选择代码 — 创建工作区时选择 default/sift 的代码位置（已读文件核实）：弹窗与按钮接管逻辑在 src/client/index.tsx 的 installWorkspaceTypeCreator（约 99–154 行，含弹窗 HTML 第 110 行、选择逻辑、pickDirectory→workspaces.create→setProfile→openWorkspace 创建流程、reconcile 克隆原生"添加工作区"按钮），挂载入口在 apply() 约 183–186 行；类型持久化在 src/host/index.ts 的 writeWorkspaceProfile（写入工作区 .sift/config.json）与 readWorkspaceProfile（缺省按 default 处理）；remote 通道在 src/remote.ts（profile 为 z.union literal default|sift，sift/setWorkspaceProfile 方法）。配套文档 docs/workspace-profile-creation.md，测试 tests/scaffold.test.ts。
- [2026-09-16 13:28] [工作记录] 实测确认 Host 半边已生效，client 渲染待浏览器验证 — 实测运行中的 Sift 开发实例（PID 48976，页面 http://127.0.0.1:9082，探针 9083）：探针 /sift 返回 hasSiftService=true、hasToolsService=true、siftTools=[sift_propose_document_change]（当前阶段只注册 1 个工具属正常）；启动 HTML 已包含 @songyanglin/dsh-sift/client.js。结论：Host 半边已生效；client 产物"会被下发"已证实，但浏览器端真实渲染未验证。client 产物本体版本标记检查未完成：/plugins/??@songyanglin/dsh-sift/client.js 在当前构建返回 404（Bearer 头与 query token 两种鉴权均试过，404/401），docs/v0.2-phase0.md 与 materials-panel.md 记录的历史产物拉取路径已失效。
- [2026-09-16 13:35] [工作记录] 类型选择弹窗未出现：产物/选择器/槽位已排除，锁定浏览器运行时 — 用户实测反馈：创建工作区未出现 default/sift 类型选择弹窗，直接进入目录选择。本轮只读排查已排除四类原因（均实测）：1) dist/client/index.js（2026-09-16 13:18 构建）含接管代码全部标记（siftWorkspaceCreator/data-sift-replaced）；2) 运行实例 13:18:31 启动（构建后 12 秒），boot graph 含 @songyanglin/dsh-sift/client.js，下发的是新产物；3) 选择器 button[aria-label="添加工作区"] 与 DSH 0.1.5-rc.2 源码一致（dsh-client-ui-workspace/lib/client.js:2310，locale workspace.add）；4) 槽位 sidebar.footer.action 存在。未排除：浏览器运行时 apply() 未跑/中途抛错、reconcile 未接管。两个新线索：原生 + 按钮包在 Tooltip 里且仅 directoryFlowAvailable 时渲染；原生创建有两条链路——图标按钮（Sift 拦截）与 WorkspacePickFlow 菜单"添加工作区…"（:1086，Sift 不拦截，若用户点的是菜单项则属实现缺口而非 bug）。"按钮接管"的手工验收在历史记录中从未标记通过。
- [2026-09-16 13:44] [工作记录] 按钮接管已实装生效，#185 无限重渲染崩溃待定位崩溃入口 — 用户强刷后反馈：控制台报 React error #185（最大更新深度超限，无限重渲染），崩溃点为 sidebar.footer.action 槽位入口（"slot entry crashed"）；DOM 实测原生添加按钮 hidden=true 且 [data-sift-create-workspace] 克隆存在。结论：apply() 正常运行、installWorkspaceTypeCreator 接管已生效，此前"直接进目录选择"大概率是强刷前的旧页面状态（旧 bundle/HMR 半更新），待用户点击 + 按钮确认弹窗。静态已排除：workspaces.list 与 sessions store 的 getSnapshot 均为脏标记+缓存稳定实现；badge 的 useEffect 依赖全为基本类型。崩溃者待定位：Sift 的 WorkspaceProfileBadge（从未在浏览器渲染成功）或同槽位 DSH 自带 cordis 面板入口。若确认是 badge 崩溃，则历史实验记录"Client contribution 未通过/类型文字不渲染"可能一直是这个崩溃被误读（此为假设，待证实）。
- [2026-09-16 13:57] [工作记录] 定位弹窗隐形（无 CSS）与克隆按钮布局位移两个 bug — 用户实机反馈两个症状并已定位根因：1) "点击添加按钮没反应"——弹窗代码实际执行了，但 data-sift-workspace-creator 在整个 src 中只有设置处一行、没有任何 CSS，overlay 是被 append 到 body 末尾的裸 div（无遮罩/无居中/无 z-index），满屏应用里不可见，造成"点了没反应"假象；2) "克隆按钮被挤到右侧"——installWorkspaceTypeCreator 采用 cloneNode 后 insertAdjacentElement(afterend) 插入 React 管理的弹性容器，外来兄弟节点导致布局位移，且与 React 重渲染冲突（此前"两个按钮都在"同源）。已确认的修复方案（待 Build 实施）：a) 给弹窗补样式（全屏半透明遮罩+居中面板+z-index）；b) 接管方式改为在原生按钮正上方盖透明点击层（实时同步按钮 rect），不再插入兄弟节点，原生按钮保持原位。改动范围：src/client/index.tsx 的 installWorkspaceTypeCreator + 新增弹窗 CSS。
- [2026-09-16 14:07] [工作记录] 弹窗样式与透明点击层接管已实施，tsc 通过，产物已热重建 — Build 实施完成（2026-09-16）：1) 新增 src/client/workspace-creator.css——弹窗全屏遮罩+居中面板+z-index，颜色用系统色（Canvas/CanvasText）跟随主题、前置普通色值回落；2) 重写 installWorkspaceTypeCreator——废弃"克隆按钮插兄弟节点"，改为原生按钮正上方同尺寸透明点击层（fixed 定位实时同步 getBoundingClientRect，零尺寸时隐藏），原生按钮不改外观不挪位；Enter/Space 键盘路径一并拦截防绕过；HMR 重复 apply 用 data-sift-creator-layer 标记防重入（detach 时摘除）；scroll/resize/keyown 监听全部用 AbortController 分级清理；弹窗补 Escape 关闭；3) 新增 tests/workspace-creator.test.ts 六用例（接管/弹窗/创建链路/取消/React 重建按钮重新接管/dispose 清理）。验证状态：tsc --noEmit 通过；pnpm build 沙箱 EPERM（已知限制）但用户侧 pnpm dev watcher 自动重建 dist 并重启实例（14:04:07 新 PID，产物含 data-sift-create-layer/siftCreatorLayer/data-sift-dialog 全部新标记）；vitest 未在沙箱执行，待用户宿主机 pnpm test。浏览器实机验证待用户刷新后点击 + 确认。
- [2026-09-16 14:09] [工作记录] commit 8d7b895：弹窗样式与透明点击层接管已提交；git push 待用户手动 — 本轮实施结果已提交 commit 8d7b895（弹窗 CSS + 透明点击层接管 + 6 个测试）。git push 因 SSH 子进程被 DSH 沙箱 EPERM 拦截，需用户手动推（git push origin main）。提交时排除了非本轮的 scripts/dev.mjs/process.mjs 改动（已有的 inspector 调试支持，不应混入）。
- [2026-09-16 14:31] [工作记录] 接入 DSH Modal：代码完成 tsc 通过，构建因包未安装受阻 — 实现完成但构建受阻：1) 新建 src/client/workspace-creator.tsx——使用 DSH Modal（@deepseek-ai/dsh-client-ui-primitives）替代手搓弹窗，createRoot 挂载 CreatorModal 组件，捕获阶段 click 拦截逻辑保持不变；2) index.tsx 导入并重导出，移除旧 creatorCss 导入，删除 workspace-creator.css；3) 添加 @deepseek-ai/dsh-client-ui-primitives 类型声明到 styles.d.ts；4) package.json inject 与 build.mjs external 已更新；5) 测试已更新（mock Modal，6 用例）；6) tsc --noEmit 通过。阻碍：@deepseek-ai/dsh-client-ui-primitives 未安装在项目 node_modules 中，esbuild 解析失败导致 watcher 构建静默失败——需要用户宿主机运行 pnpm add -D @deepseek-ai/dsh-client-ui-primitives@0.1.5-rc.2 后方可重建产物。
- [2026-09-16 14:46] [工作记录] 修复 sidebar.footer.action Badge React #185 崩溃 — 修复 WorkspaceProfileBadge 侧边栏 Badge 全链路：1) React #185 无限重渲染崩溃——emptyWorkspaces/emptySessions 改为模块级常量（已确认崩溃消除）；2) Badge 始终显示"未选择工作区"——{...ctx} spread 丢失 Cordis 响应式服务，改为 Object.create(ctx, { remote: { value: { sift: ... } } }) 保留原型链。tsc 通过。用户实机已验证 Badge 正常工作。
- [2026-09-16 15:41] [工作记录] Reference 模块现状与待实现链路梳理 — 完整梳理参考模块四层：1) 数据壳——SPIKE_RECORDS 硬编码三条（待 Phase 5 删除），Reference Board 传 cards:[]；2) 视图——board.ts 空状态+卡片渲染、panel.ts 左栏拼装、source/drawer.ts 四种来源管理（工作正常，onSelectionChange 回调无人接）；3) 聊天输入集成——source.ts @输入触发器、codec.ts 两种投影、input-trigger.ts DSH 适配（已实现）；4) Host 端——source/store.ts 读写 Source 索引。关键断链：Source Drawer 选中来源后未流入 Reference Board。待实现：Phase 4 整份加入参考（读取文件内容→生成卡片）或 Phase 7 AI 提取参考。

## 经验教训 Lessons Learned

- [2026-09-16 11:18] [经验教训] DSH 沙箱环境限制：无法运行 spawn/exec 子进程 — DSH 沙箱（workspace-write 模式下仍受限）拦截 Node.js 的 spawn/exec 调用（EPERM），导致 vitest（worker pool）、esbuild（native binary）、git push（SSH 子进程）均无法在沙箱内正常运行。规避方式：tsc –noEmit 可用于类型检查（无子进程），其余需在宿主机终端手动执行或通过 DSH 沙箱外部环境。
- [2026-09-16 13:28] [经验教训] 插件生效判定：探针 /sift 是 Host 证据，UI 行为是 Client 唯一标准 — 判断 dsh-sift 插件是否生效的方法：1) Host 半边最可靠证据是探针 GET /sift（读 .debug/development/sift-runtime.json 取 probePort 和 Bearer token），返回 hasSiftService 与 siftTools 列表；pnpm dev:status 只打 /state，仅证明 dev 进程活着，不是插件级证据。辅助信号：host 日志 "Sift 插件已加载。"（日志名显示为 SiftService 而非 sift）与工作区目录下 .sift/config.json。2) Client 半边"加载了"≠"生效了"：插件清单"运行中"+启动图含 client.js 都只是必要条件（docs/workspace-profile-experiment.md 已踩过此坑）；最终标准只有真实 UI 行为——新建工作区出现 default/sift 弹窗、sift 工作区出现三栏（参考|文档|对话）；锚点失败时控制台报 "Sift: 未找到 DSH center column 锚点，已跳过三栏布局。"。3) 防"假生效"：dev 是 link: 到 dist/，pnpm dev 没跑时改代码实例一直发旧产物，看 watcher 输出确认重建；sift-runtime.json 被最后一次 pnpm dev 覆盖，多实例时 dev:status 指向会漂，重启 pnpm dev 对齐。
- [2026-09-16 13:28] [经验教训] 沙箱受限模式与产物拉取路径的两个验证坑 — DSH 沙箱 ConstrainedLanguage 模式两个坑：1) Get-NetTCPConnection 静默返回空结果（不报错），查端口监听要用 netstat -ano 代替；[Math]::Min、Substring/Split 等方法调用也会被拦。2) fetch 运行中 DSH 实例的 /plugins/??@songyanglin/dsh-sift/client.js 在当前构建返回 404——历史 docs 记录的"拉取产物验证版本标记"路径已失效，替代方案待定（可改为直接查磁盘 dist/ 产物 + 重启时间对比，或浏览器 DevTools）。
- [2026-09-16 13:35] [经验教训] DSH 创建工作区双链路与 cordis 面板诊断入口 — DSH 0.1.5-rc.2 客户端侧可复用诊断事实：1) 原生"创建工作区"有两条链路：侧边栏 + 图标按钮（aria-label=添加工作区，由 Tooltip 包裹、仅 directoryFlowAvailable 时渲染）与 WorkspacePickFlow 菜单项"添加工作区…"（menu.addWorkspace，带省略号）——只接管图标按钮时菜单链路不被拦截。2) 侧边栏底部 cordis 插件面板（dsh-client-ui-cordis，同样注册在 sidebar.footer.action）会内联显示每个 client 插件"没装上"还是"装上后抛错"及错误信息，是排查 client contribution 激活问题的一级诊断入口。3) 排查 client 未生效先分叉：sidebar 徽标（data-dsh-sift-profile）在不在 → 不在查 cordis 面板/控制台，在则查 DOM（原生按钮 hidden、data-sift-create-workspace 克隆是否存在）。
- [2026-09-16 13:44] [经验教训] DSH store 快照稳定可安全用于 useSyncExternalStore；按槽位入口隔离崩溃 — DSH client store 可复用事实：dsh-api-workspace-controller 与 dsh-api-session-controller 的 list 快照均为"脏标记 + 缓存"实现（refreshSnapshot/ensureFresh 仅在 dirty 时 rebuild，返回 snapshotCache），getSnapshot 返回稳定对象引用，可直接安全用于 React useSyncExternalStore；排查 #185 时不必怀疑这两个 store 的快照稳定性。sidebar.footer.action 席位上有两个入口：Sift WorkspaceProfileBadge 与 DSH 自带 cordis 面板（dsh-client-ui-cordis）；槽位崩溃按入口隔离（"slot entry crashed"），用 cordis 面板能否打开区分崩溃者——能打开则看 sift 行内联错误确认 badge 崩溃，打不开则崩的是 cordis 入口本身。
- [2026-09-16 13:49] [经验教训] 浏览器自动化路径：MCP 无、Playwright 被沙箱拦、chromedriver+WebDriver HTTP 可行 — 浏览器自动化能力结论（2026-09-16 实测核查）：1) 当前会话无 MCP 客户端、无浏览器工具，工具面固定为 pwsh/文件/网络抓取；DSH 发行包内 MCP 相关代码全部来自内嵌 SDK（ACP/Anthropic/GenAI），不是可挂载的 MCP 客户端。2) Playwright 在 DSH 沙箱内跑不通：依赖 Node child_process 拉起 worker 与浏览器（默认 --remote-debugging-pipe 管道），与 vitest 失败同因（EPERM 红线），且 repo 未安装 Playwright。3) 本机有 Edge（Program Files (x86)\Microsoft\Edge\Application\msedge.exe）与 Chrome（Program Files\Google\Chrome\Application\chrome.exe），无 chromedriver/msedgedriver。4) 可行方案：chromedriver + WebDriver 纯 HTTP——Start-Process 启动 chromedriver（不经 Node），浏览器由 driver 自己拉起，Invoke-RestMethod 走 WebDriver 协议完成导航/执行 JS/DOM 查询/抓 console 日志/截图，可绕开 Node spawn 红线。约束：Ask 只读模式下下载 chromedriver 写盘会被拦，需 Build 模式实施；备选是用户宿主机跑 Playwright、AI 写脚本分析结果。
- [2026-09-16 14:07] [经验教训] 接管 React 管理的按钮：透明点击层取代克隆插入 — 可复用实现经验：接管 React 管理的 DOM 元素（如 DSH 原生按钮）时，不要 cloneNode 后插入兄弟节点——外来节点会被 flex/grid 挤偏位、且与 React 协调冲突（本轮"按钮挤到右侧/两个按钮并存"的根因）；正确模式是原生元素保持原位，在其正上方覆盖 fixed 定位的透明点击层，getBoundingClientRect 实时同步（scroll capture/resize/MutationObserver 触发），键盘路径在原元素上 preventDefault 拦截；重复接管用 dataset 标记防重入并在 detach 时摘除，监听器统一 AbortController 分级（安装级/接管级/弹窗级）清理。另外：写盘后无需手动构建——用户侧 pnpm dev watcher 会自动重建 dist 并重启 DSH 实例，AI 只需对比 dist mtime 与标记确认。
- [2026-09-16 14:17] [经验教训] 捕获阶段 click 拦截：接管 React DOM 按钮的最简模式 — 最终确定的最干净接管模式：拦截 React 管理的原生按钮不要修改 DOM，用 document.addEventListener('click', handler, true) 在捕获阶段拦截点击——closest('button') + isWorkspaceAddButton() 判断目标，preventDefault + stopPropagation 阻止原生流程，show() 弹对话框。零 DOM 插入/零布局影响/零协调冲突，React 重渲染自动跟随无需重新绑定（监听在 document 上），dispose 只需 removeEventListener。selectors.ts 加 isWorkspaceAddButton 导出函数，选择器字符串保持集中。此模式同时拦截键盘 Enter/Space（浏览器会自动合成 click，捕获阶段一并拦截）。
- [2026-09-16 14:31] [经验教训] DSH 外部 client 包：inject + external + node_modules 安装三者缺一不可 — 使用 DSH 外部 client 包（如 @deepseek-ai/dsh-client-ui-primitives）需要同时做三件事：1) 加入 dsh.client.inject（模块加载器注册）；2) 加入 esbuild external（防止构建时打包，产物留下 require）；3) 必须在项目 node_modules 中有该包（否则 esbuild 解析不到 bare specifier 直接失败）。即使它是运行时 external，esbuild 也需要 RESOLVE bare specifier 才能决定不打包它——不存在时不会默默跳过而是抛错终止。只改 inject + external 不够，必须在 devDependencies 中安装该包。此版本对应 dsh-client-ui-primitives@0.1.5-rc.2。
- [2026-09-16 14:54] [经验教训] useSyncExternalStore getSnapshot 必须返回稳定引用 — 在 React 18+ 中，useSyncExternalStore 的 getSnapshot 必须返回稳定引用（Object.is 比较）。如果每次返回新对象（如 { phase: 'pending' } 字面量），React 会认为状态持续变化，触发无限重渲染，最终抛 React error #185（suspended during synchronous input）。修复方法：getSnapshot 的 fallback 用模块级常量代替函数返回值。
- [2026-09-16 15:09] [经验教训] Cordis v4 ctx 不可用 {...ctx} spread 拷贝 — Cordis v4 的 ctx 是 Proxy/getter 实现，{...ctx} spread 无法正确拷贝注入的服务（workspaces/sessions 等响应式对象）。必须用 Object.create(ctx, { overrideProperty: { value: ... } }) 创建子对象，保留原型链，让服务访问时动态解析。

## 行动指南 Action Guide

- [2026-09-16 11:18] [行动指南] 下一步：Plan 模式制定 v0.2 Phase 1 重新实施计划 — 从 6add38e 起点开始重新实现 v0.2。下一轮切换到 Plan 模式后，按实施文档 §45 制定 Phase 1 计划：Workspace Profile 识别 + dsh-adapter 拆为四个文件 + 三栏布局（参考|文档|对话）+ Untitled Document。用户需先在终端手动执行 git push --force origin main 更新远程。
- [2026-09-16 13:28] [行动指南] 浏览器实机验证 client 半边渲染 — 待办：1) 宿主机执行 node scripts/build.mjs 完成重建（dist 仍是旧产物含 creatorCss，新代码已不含），或删 dist/client/ 后重启 pnpm dev；2) 重建后刷新 9082 验证 Modal 弹出与类型选择；3) 创建 sift 工作区验证三栏；4) 宿主机 pnpm test；5) 用户 git push origin main；6) 定位 #185。菜单"添加工作区…"链路未接管，另立任务。此前"Plan 模式制定 v0.2 Phase 1 重新实施计划"行动仍然有效。
- [2026-09-16 15:47] [行动指南] 清除参考模块与 Source Drawer 重新实现 — 已清除参考模块和 Source Drawer 全部代码。删除 7 个文件（src/client/reference/ 5 文件、src/client/source/ 2 文件），恢复 src/sources.ts（Host 端依赖）。清理 src/client/index.tsx 中 10 处引用/数据/调用。tsc 通过。左栏现显示占位文本"参考面板（待实现）"，后续重新设计参考模块架构。
