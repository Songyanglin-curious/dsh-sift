1. 参考去标签卡区域没有进行固定
2. 想法组合的文本最后加一个-------- 这样的分割线
3. `sift_propose_document_change` 走 Diff，你逐处接受/拒绝  未适配
4. 没有文档刷新机制  文档被AI改了没有进行同步
5. 想法的添加改为Enter就行和codex一致
6. Document区域有无必要进行编辑使用dsh原生的方式是不是也可以这样一来 是不是就能走diff了？
